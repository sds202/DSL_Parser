<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DSL_parser_winui3.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DSL_parser_winui3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DSL_parser_winui3">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer x:Name="ChatScrollViewer" Grid.Row="0" Margin="20" VerticalScrollBarVisibility="Hidden">
            <StackPanel x:Name="ChatPanel" Spacing="12"/>
        </ScrollViewer>

        <Grid Grid.Row="1" Margin="20" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="MessageInput" PlaceholderText="Type a message..." KeyDown="OnInputKeyDown" FontSize="16" Height="45" VerticalAlignment="Center" VerticalContentAlignment="Center" Padding="12,8,12,0"/>
            <Button Grid.Column="1" Content="Send" Click="OnSendClick" FontSize="16" Height="45" Padding="20,0,20,0" VerticalAlignment="Center" Style="{StaticResource AccentButtonStyle}"/>
        </Grid>
    </Grid>
</Window>
